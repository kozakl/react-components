@import '../../variables.pcss';

.checkboxSample {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-top: 2rem;
  width: 10em;
  
  & > :first-child {
    margin-bottom: 2rem;
  }
  & > :last-child {
    margin-top: 2rem;
  }
}
