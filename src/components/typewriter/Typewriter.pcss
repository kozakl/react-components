.typewriter {
  @for $i from 1 to 11 {
      & :nth-child($i) {
        opacity: 0;
        transition: opacity 0.1s linear calc($i * 0.05s);
      }
  }
  &.show {
    & > span {
      opacity: 1;
    }
  }
}
